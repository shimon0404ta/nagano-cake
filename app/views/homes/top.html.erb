<div class="container">
  <div class="row py-5">
    <div class="col-lg-2">
      <table class="table" border="1">
          <tr>
            <th>ジャンル検索</th>
          </tr>

          <tr>
            <td>
              <% @genres.each do |genre| %>
              <%=link_to genre.genre, root_path %><br>
              <% end %>
              </td>
          </tr>
      </table>
    </div>

    <div class="col-lg-8 shadow p-3 mb-5 bg-white rounded">
      <p>ようこそ長野ケーキへ！</p>
      <p>このサイトは、ケーキ販売のECサイトになります。</p>
      <p>会員でない方も商品の閲覧はできますが、</p>
      <p>購入には会員登録が必要になります。</p>
      <%= image_tag 'about_cake.jpg' %>
    </div>
  </div>

  <div class="row">
    <div class="col-lg-2">
    </div>

    <div class="col-lg-10">
      <h4>新着商品</h4>
    </div>
  </div>

  <div class="row">
    <div class="col-lg-2">
    </div>

    <% @items.each do |item| %>
      <div class="col-lg-2">
        <%= link_to image_tag(item.get_item_image(150,100)), item_path(item.id) %><br>
          <%= item.name %><br>
        <%#= item.price %>
        <!--¥表記、引数に小数点の非表示化 defaultはfalseになるが非表示としたいので、true-->
        <%= number_to_currency(item.price, unit: "¥", strip_insignificant_zeros: true) %>
      </div>
    <% end %>
  </div>

</div>
